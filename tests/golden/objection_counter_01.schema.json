{
  "type": "object",
  "required": ["objections"],
  "properties": {
    "objections": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "required": ["hear", "say"],
        "properties": {
          "hear": {"type": "string", "minLength": 1},
          "say": {"type": "string", "minLength": 1}
        }
      }
    }
  },
  "_note": "Mirrors the objections[] shape embedded in agents/warm_followup.py's booked-call prep prompt (feature='reply'). Price objection scenario, exactly 2 hear/say pairs expected."
}
