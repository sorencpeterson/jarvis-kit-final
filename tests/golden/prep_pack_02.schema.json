{
  "type": "object",
  "required": ["opener", "questions", "objections", "close"],
  "properties": {
    "opener": {"type": "string", "minLength": 1},
    "questions": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string"}
    },
    "objections": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["hear", "say"],
        "properties": {
          "hear": {"type": "string"},
          "say": {"type": "string"}
        }
      }
    },
    "close": {"type": "string", "minLength": 1}
  },
  "_note": "Same prep-pack shape as prep_pack_01, e-commerce boutique niche this time. Catches niche-specific prompt drift on the same schema."
}
