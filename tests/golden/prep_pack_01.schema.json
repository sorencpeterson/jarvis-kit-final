{
  "type": "object",
  "required": ["opener", "questions", "objections", "close"],
  "properties": {
    "opener": {"type": "string", "minLength": 1},
    "questions": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string"}
    },
    "objections": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["hear", "say"],
        "properties": {
          "hear": {"type": "string"},
          "say": {"type": "string"}
        }
      }
    },
    "close": {"type": "string", "minLength": 1}
  },
  "_note": "Mirrors agents/warm_followup.py's booked-call prep prompt exactly (feature='reply'), the 'dispo=booked' path that builds store/prep/sales_<wid>.md. Local-service niche."
}
