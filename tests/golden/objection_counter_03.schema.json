{
  "type": "object",
  "required": ["objections"],
  "properties": {
    "objections": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "required": ["hear", "say"],
        "properties": {
          "hear": {"type": "string", "minLength": 1},
          "say": {"type": "string", "minLength": 1}
        }
      }
    }
  },
  "_note": "\"Is this a template\" + post-launch changes/scope objections, e-com niche. Third variant of the same hear/say shape."
}
